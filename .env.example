# Database Configuration
# Get these values from your Vercel Postgres database
# See: https://vercel.com/docs/storage/vercel-postgres/quickstart

# Prisma connection URL (uses connection pooling)
POSTGRES_PRISMA_URL="postgres://default:password@host/database?sslmode=require&pgbouncer=true"

# Direct connection URL (for migrations)
POSTGRES_URL_NON_POOLING="postgres://default:password@host/database?sslmode=require"

# Session Configuration
SESSION_SECRET="your-secure-random-session-secret-here-change-in-production"

# Environment
NODE_ENV="development"
